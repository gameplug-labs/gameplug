name: Test Build

on:
  push:
    branches: [ main, dev, upscaler ]
  pull_request:
    branches: [ main, dev, upscaler ]

jobs:
  test-build:
    name: Test Build ${{ matrix.arch }}
    runs-on: windows-latest
    strategy:
      matrix:
        arch: [Win32, x64]

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Install Vulkan SDK
        uses: jakoch/install-vulkan-sdk-action@v1.1.1
        with:
          vulkan-sdk-version: 1.3.268.0
          vulkan-sdk-components: 'Vulkan-Headers, Vulkan-Loader'
          vulkan-sdk-cache: false

      - name: Configure CMake
        run: cmake -B build -A ${{ matrix.arch }}

      - name: Build
        run: cmake --build build --config Release
